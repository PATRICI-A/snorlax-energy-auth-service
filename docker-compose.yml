version: '3.8'
services:
  auth-service:
    build: ../snorlax-energy-auth-service
    container_name: patricia-auth-service
    ports:
      - "8080:8080"
    environment:
      - MONGO_URI=${MONGO_URI}
      - JWT_SECRET=${JWT_SECRET}
    env_file:
      - .env
    restart: unless-stopped
